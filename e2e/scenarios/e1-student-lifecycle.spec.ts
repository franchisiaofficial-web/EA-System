import { test, expect } from "../fixtures/auth.fixture";

test.describe("E1 — Student Lifecycle", () => {

  test("E1A — Create Student", async ({ authenticatedAdmin }) => {
    // TODO: Implement when infrastructure recovers
  });

  test("E1B — Edit Student", async ({ authenticatedAdmin }) => {
    // TODO
  });

  test("E1C — Archive Student", async ({ authenticatedAdmin }) => {
    // TODO
  });

  test("E1D — Student Profile", async ({ authenticatedAdmin }) => {
    // TODO
  });
});
