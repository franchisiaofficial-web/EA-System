import { test, expect } from "../fixtures/auth.fixture";

test.describe("E4 — Search", () => {
  test("E4A — Search by Name", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E4B — Search by Admission Number", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E4C — Search with Pagination", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E4D — Search Empty Results", async ({ authenticatedAdmin }) => { /* TODO */ });
});
