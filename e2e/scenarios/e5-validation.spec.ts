import { test, expect } from "../fixtures/auth.fixture";

test.describe("E5 — Validation", () => {
  test("E5A — Missing Required Fields", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E5B — Invalid Date Format", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E5C — Invalid Admission Number", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E5D — Guardian Validation", async ({ authenticatedAdmin }) => { /* TODO */ });
});
