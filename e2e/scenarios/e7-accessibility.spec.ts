import { test, expect } from "../fixtures/auth.fixture";

test.describe("E7 — Accessibility", () => {
  test("E7A — Student List Page", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E7B — Student Profile Page", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E7C — Guardian Dialog", async ({ authenticatedAdmin }) => { /* TODO */ });
  test("E7D — Create Student Form", async ({ authenticatedAdmin }) => { /* TODO */ });
});
